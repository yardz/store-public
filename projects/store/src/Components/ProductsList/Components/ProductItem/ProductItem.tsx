import React from 'react';
import { Product } from '@lab77store/domain';

// import { Image } from 'Components/Image';

interface Props {
	product: Product;
	size: {
		width: number | 'auto';
		height: number | 'auto';
	};
}

export const ProductsItem: React.FC<Props> = () => (
	// Display do card de produto dentro da lista
	// Le
	<>ProductsItem</>
);
