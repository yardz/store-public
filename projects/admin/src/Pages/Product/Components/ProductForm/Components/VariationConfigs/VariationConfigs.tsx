import React from 'react';
export const VariationConfigs: React.FC = () => {
	return null;
};

// import { ProductVariationsConfig } from '@lab77store/database';

// import * as Components from './Components';

// interface Props {
// 	prefix: string;
// 	variationsConfig: ProductVariationsConfig;
// 	// tslint:disable-next-line: no-any
// 	setValues: (field: string, value: any, shouldValidate?: boolean | undefined) => void;
// }

// export const VariationConfigs: React.FC<Props> = ({ prefix, variationsConfig, setValues }) => {
// 	return (
// 		<div className="tile">
// 			<Components.ButtonCreateVariations fieldName={'variations'} variationsConfig={variationsConfig} setValues={setValues} />
// 			<hr />

// 			<Components.AtributeGrid prefix={prefix} variationsConfig={variationsConfig} setValues={setValues} />
// 			<hr />

// 			<Components.SelectProductionProcess prefix={prefix} />
// 		</div>
// 	);
// };
